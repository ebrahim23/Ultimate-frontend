- var pageTitle = 'about';
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{pageTitle}
        include pug/layout/styles
    body
        .page.home
            include pug/components/rightbar
            include pug/layout/sidebar
            .content
                .content-head
                    include pug/layout/header
                    include pug/layout/sub-content
    include pug/layout/scripts